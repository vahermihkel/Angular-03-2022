import { ExtendInputDirective } from './extend-input.directive';

describe('ExtendInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtendInputDirective();
    expect(directive).toBeTruthy();
  });
});
